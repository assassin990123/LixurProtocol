First, we need to make sure that we can generate the genesis transactions properly.
Once that is done, consensus is needed. Work on that.
Then check on wallet accessing and sending LXR.

Once that is done, we can 

Generate readable addresses once everything is operating properly.
Also, allow proof of dedication for weights.
Transaction Type, Transaction Status, Readable Hash, Directory Index, Payload, Directory, Memo
